"use client";
import React from "react";
import CountUp from "react-countup";

function AnimatedCountUp({ amount }: { amount: number }) {
  // Hooks

  // Backend Calls

  // Refs and States

  // Emits

  // Functions

  return (
    <div className="w-full">
      <CountUp end={amount} prefix="Rs " decimalPlaces={4} />
    </div>
  );
}

export default AnimatedCountUp;
